name: CI

on:
  push:
    branches: [main, master]
  pull_request:

jobs:
  lint-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install uv
        uses: astral-sh/setup-uv@v5
      - name: Check requirements export matches lock
        run: |
          uv export --frozen --no-dev --no-hashes -o /tmp/requirements.txt
          uv export --frozen --no-hashes -o /tmp/requirements-dev.txt
          diff -u -I '^#' requirements.txt /tmp/requirements.txt
          diff -u -I '^#' requirements-dev.txt /tmp/requirements-dev.txt
      - name: Install dev dependencies
        run: pip install -r requirements-dev.txt
      - name: Ruff
        run: ruff check
      - name: Pytest (unit)
        run: pytest -m "not integration" --cov=src --cov-report=term-missing --cov-fail-under=55

  image:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Docker build
        run: docker build -t ovirt-analyzer:ci .
