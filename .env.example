# === Подключение к PostgreSQL oVirt Engine ===
# Если БД на том же хосте, где запускается Docker:
DB_HOST=host.docker.internal
# Если БД на удаленном сервере, укажите IP:
# DB_HOST=192.168.1.50

DB_PORT=5432
DB_NAME=engine
DB_USER=postgres
DB_PASSWORD=changeme